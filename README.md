# HandOnMe Backend

Backend API for HandOnMe - A local book exchange marketplace for Nepal.

## Tech Stack

- Node.js + Express
- MongoDB + Mongoose
- JWT Authentication
- Socket.io for real-time chat

## Setup

1. Clone the repo
2. Run `npm install`
3. Create `.env` file (see `.env.example`)
4. Run `npm run dev`
